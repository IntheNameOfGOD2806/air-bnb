import CommonModal from ".";

const AuthModal = () => {
  return (
    <div>
      <CommonModal
        title="Login"
        visible={false}
        onCancel={() => {}}
        onOk={() => {}}
        okText="Login"
        cancelText="Cancel"
        style={{}}
      >
        adada
      </CommonModal>
    </div>
  );
};

export default AuthModal;
